import bpy
import io

def obj_import_bone_name(bone_name)
    for auxBone, boneName in enumerate(boneAux):
        
        actualName = 
        create_bpy(boneName, 
        armature_obj,data)
                boneAux[auxBone]=boneName
